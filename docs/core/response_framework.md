# ResponseFramework

Turbine-level response surrogates: loads, fatigue, and damage accumulation
consumed by lifetime and O&M modules.

```{note}
Stub — the `ResponseFramework` lives in `core/ResponseFramework/` as a
sub-package. Document the individual surrogate modules and add them to the
API autosummary list once structure stabilises.
```
