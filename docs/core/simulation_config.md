# SimulationConfig

The `SimulationConfig` object is the single entry point describing a WINPACT run:
inputs, time horizon, modules to activate, and output destinations.

See {py:class}`core.SimulationConfig.SimulationConfig`.

```{note}
Stub — add YAML schema reference, minimal example, and diagram of how
`SimulationConfig` fans out into the other core objects.
```
