# WindFarm

Layout, turbine definitions, and wake coupling to PyWake.

See {py:mod}`core.WindFarm`.

```{note}
Stub — document layout input formats (CSV, windIO), turbine specs, and the
link to `PyWakeModel` for wake-aware AEP.
```
