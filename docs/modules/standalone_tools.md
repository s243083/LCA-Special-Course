# Standalone Tools

Scripts and utilities in `winpact/core/Standalone_tools` that can be used
independently of a full simulation.

```{note}
Stub — enumerate tools (BladeMassEstimator, etc.) with inputs/outputs.
```
