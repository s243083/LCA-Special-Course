# Theory & Methodology

```{toctree}
:maxdepth: 1

economic_metrics
response_surrogates
market_coupling
```

```{note}
Stub — populate with governing equations, assumptions, and citations.
MathJax is enabled via `dollarmath` / `amsmath` MyST extensions, so
LaTeX like `$\mathrm{LCOE} = \frac{\sum_t C_t/(1+r)^t}{\sum_t E_t/(1+r)^t}$`
will render directly.
```
